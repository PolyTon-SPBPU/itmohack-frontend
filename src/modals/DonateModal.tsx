import { FC } from "react";
import { Text } from "../ui";
import { Group } from "@vkontakte/vkui";

export const DonateModal: FC = () => {
  return (
    <Group>
      <Text>Еще находится в разработке</Text>
    </Group>
  );
};
